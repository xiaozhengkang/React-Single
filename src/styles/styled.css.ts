import {style} from '@vanilla-extract/css';

export const todoList = style({
    marginTop: '20px',
    background: '#ccc',
    color: 'red',
});

export const todoInfo = style({
    paddingTop: '10px',
});